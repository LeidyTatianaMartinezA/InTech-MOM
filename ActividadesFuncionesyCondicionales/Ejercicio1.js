//Iterar el array
const weekDays = ['lunes', 'martes', 'miercoles', 'jueves', 'viernes'];

for (let i = 0; i < weekDays.length; i++) {
    if (i = weekDays[3]){
        console.log("llegamos al día "+i);
    }
}